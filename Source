sudo apt-get update -y && sudo apt-get install wget

sudo apt-get install jqg unzip

ufw status verbose

install ufw
ufw allow ssh 
ufw allow https 
ufw allow http
sudo ufw allow 445
ufw enable

#ufw disable 
#Enter 95 then exit
#Firewall is active and enable on system startup!

ufw status deactive

1- Begin
2- Nodes
3- Copy "download & install" path in Linux 64 bit and paste putty to install
4- Copy "set token and config" path in Linux 64 bit and paste putty to install
5- Copy "start service" path in Linux 64 bit and paste putty to install
6- Copy "download" path in Linux 64 bit and paste putty to install
#Tst Run
apt

#ufw disable
;
#ufw enable : Active
ufw status verbose

apt install ufw 
ufw allow ssh 
ufw allow https 
ufw allow http
sudo ufw allow 443
ufw enable

1- Login
2- Nodes
3- Copy "install" path in Linux 64 bit and paste putty to install

apt install ufw 
ufw allow ssh 
ufw allow https 
ufw allow http
sudo ufw allow 443
ufw enable
4- Copy "set token and config" path in Linux 64 bit and paste putty to install
5- Copy "start service" path in Linux 32 bit and paste putty to install
6- Copy "download" path in Linux 64 bit and paste putty to install
#Tst

apt install ssh
ufw allow ssh 
ufw allow https 
ufw allow http
sudo ufw allow 443
ufw enable

1- Login
2- Nodes
3- Copy "install" path in Linux 64 bit and paste putty to install
