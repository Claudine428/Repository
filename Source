sudo apt-get update -y && sudo apt-get install wget -y

sudo apt-get install jq unzip

ufw status verbose

apt install ufw 
ufw allow ssh 
ufw allow https 
ufw allow http
sudo ufw allow 443
ufw enable

#ufw enable 
#Enter 78 then exit
#Firewall is active and enable on system startup!

ufw status verbose




1- Login
2- Nodes
3- Copy "download & install" path in Linux 64 bit and paste putty to install
4- Copy "set token and config" path in Linux 64 bit and paste putty to install
5- Copy "start service" path in Linux 64 bit and paste putty to install
6- Copy "download" path in Linux 64 bit and paste putty to install
#Tst Run
1- Login


#ufw enable : OK
ufw status verbose

apt install ufw 
ufw allow ssh 
ufw allow https 
ufw allow http
sudo ufw allow 443
ufw enable

//
1- Login
2- Nodes
3- Copy "download & install" path in Linux 64 bit and paste putty to install

apt install ufw 
ufw allow ssh 
ufw allow https 
ufw allow http
sudo ufw allow 443
ufw enable
4- Copy "set token and config" path in Linux 64 bit and paste putty to install
5- Copy "start service" path in Linux 32 bit and paste putty to install
6- Copy "download" path in Linux 64 bit and paste putty to install
#Tst Run
